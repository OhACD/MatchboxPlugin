# Changelog

Keep `CHANGELOG.md` in the repository as the canonical release history.

For each release on the wiki:
- Add a short summary line and link back to the full `CHANGELOG.md` for details.
- Keep historical details in the repo file to simplify PR review.

(Recent release: 0.9.5 — see `CHANGELOG.md` in repo for full details.)
